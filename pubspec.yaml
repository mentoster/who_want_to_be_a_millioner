name: who_want_to_be_a_millioner
description: who_want_to_be_a_millioner

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get:

  cupertino_icons: ^1.0.2
  video_player: ^2.2.3

dev_dependencies:

  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  audioplayers: ^0.20.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sounds/
    - assets/videos/
